const form=document.querySelector('#login-form'),error=document.querySelector('#login-error'),localeControl=document.querySelector('#ui-locale');let uiLocale=WhackSmackerUiLocale.installSelector(localeControl,locale=>{uiLocale=locale;error.textContent=''});form.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';const button=form.querySelector('button');button.disabled=true;try{const response=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:form.username?.value||'',password:form.password.value})});const body=await response.json();if(!response.ok)throw new Error(response.status===401?WhackSmackerUiLocale.translate(uiLocale,'login.incorrect'):body.error||WhackSmackerUiLocale.translate(uiLocale,'login.error'));const requested=new URLSearchParams(location.search).get('returnTo');let destination='/app';try{const parsed=new URL(requested||'/app',location.origin);if(parsed.origin===location.origin&&parsed.pathname==='/app')destination=parsed.pathname+parsed.search}catch{}location.assign(destination)}catch(reason){error.textContent=reason.message;button.disabled=false}});
